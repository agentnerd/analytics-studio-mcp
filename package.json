{
  "name": "analytics-studio-mcp",
  "version": "1.0.0",
  "type": "module",
  "description": "Analytics Studio MCP App: minimal interactive charts from actual tool-provided data",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check public/app.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@modelcontextprotocol/ext-apps": "^1.7.2",
    "cors": "^2.8.5",
    "express": "^4.19.2"
  }
}
